/**
 * Workbench shell for MinDis. In M1 this module receives the in-repo fork of
 * WorkbenchFX (PLAN.md section 4.1). Until then it is an empty placeholder.
 */
module org.mindis.workbench {
    requires javafx.controls;
}
