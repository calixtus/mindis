// Exposes the gradlex packaging plugin to application projects; target and
// installer configuration stays in the applying project (mindis-gui).
plugins {
    id("org.gradlex.java-module-packaging")
}
