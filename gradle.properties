# JavaFX version, read by the versions platform and the feature.javafx
# convention plugin. Override with -PjavafxVersion=...
javafxVersion=26.0.1

# Application version (jpackage/MSI requires plain x.y.z, no suffixes).
version=0.6.0
